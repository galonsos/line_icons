name: line_icons_demo
description: A demo for the LineIcons library

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  line_icons: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter